<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:section title="${%Default notification URL}" description="Select the default notification URL when clicking links to Jenkins from notifications (e.g. Slack, GitHub, or Email). Users can override this value as desired in their preferences.">
      <f:entry field="providerId">
        <f:select/>
      </f:entry>
    </f:section>
</j:jelly>
